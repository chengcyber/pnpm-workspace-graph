lockfileVersion: '6.0'

settings:
  autoInstallPeers: true
  excludeLinksFromLockfile: false

dependencies:
  ui:
    specifier: 1.0.0
    version: link:../../components/ui

devDependencies:
  config:
    specifier: workspace:*
    version: link:../../config
